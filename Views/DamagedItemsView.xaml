<UserControl x:Class="HamoPos.Views.DamagedItemsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200"
             FlowDirection="RightToLeft">

    <Grid Margin="20,16">
        <Grid.RowDefinitions>
            <!-- 0: Header -->
            <RowDefinition Height="Auto" />
            <!-- 1: KPI Stats Bar -->
            <RowDefinition Height="Auto" />
            <!-- 2: Main Workspace (Form + List) -->
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 0: Header -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Button Style="{StaticResource ActionButton}" Command="{Binding BackToMainCommand}" Padding="14,8" Margin="0,0,14,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🔙 " FontSize="14" />
                        <TextBlock Text="{Binding Loc[Nav_BackToMain]}" FontWeight="Bold" />
                    </StackPanel>
                </Button>
                <TextBlock Text="⚠️ " FontSize="22" />
                <TextBlock Text="{Binding Loc[Dam_Title]}" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center" />
            </StackPanel>
        </Grid>

        <!-- 1: KPI Stats Bar -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{DynamicResource BgDarkSecondary}" CornerRadius="12" BorderBrush="{DynamicResource BorderDark}" BorderThickness="1" Padding="14" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock Text="{Binding Loc[Nav_DamagedItems]}" FontSize="12" Foreground="{DynamicResource TextSecondary}" />
                    <TextBlock Text="{Binding TotalDamagedCount, StringFormat='{}{0:N0}'}" FontSize="20" FontWeight="Black" Foreground="{DynamicResource DangerBrush}" Margin="0,4,0,0" />
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Background="{DynamicResource BgDarkSecondary}" CornerRadius="12" BorderBrush="{DynamicResource BorderDark}" BorderThickness="1" Padding="14" Margin="4,0">
                <StackPanel>
                    <TextBlock Text="{Binding Loc[Dam_TotalLoss]}" FontSize="12" Foreground="{DynamicResource TextSecondary}" />
                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                        <TextBlock Text="{Binding TotalLossValue, StringFormat='{}{0:N0}'}" FontSize="20" FontWeight="Black" Foreground="{DynamicResource WarningBrush}" />
                        <TextBlock Text="{Binding Loc[App_Currency_Short], StringFormat=' {0}'}" FontSize="13" FontWeight="Bold" Foreground="{DynamicResource WarningBrush}" VerticalAlignment="Bottom" Margin="4,0,0,2" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="{DynamicResource BgDarkSecondary}" CornerRadius="12" BorderBrush="{DynamicResource BorderDark}" BorderThickness="1" Padding="14" Margin="8,0,0,0">
                <StackPanel>
                    <TextBlock Text="{Binding Loc[Pur_History]}" FontSize="12" Foreground="{DynamicResource TextSecondary}" />
                    <TextBlock Text="{Binding DamagedList.Count, StringFormat='{}{0}'}" FontSize="20" FontWeight="Black" Foreground="{DynamicResource PrimaryAccent}" Margin="0,4,0,0" />
                </StackPanel>
            </Border>
        </Grid>

        <!-- 2: Main Workspace -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <!-- Form (35%) -->
                <ColumnDefinition Width="3.5*" />
                <!-- List (6.5%) -->
                <ColumnDefinition Width="6.5*" />
            </Grid.ColumnDefinitions>

            <!-- Form Card -->
            <Border Grid.Column="0" Background="{DynamicResource BgDarkSecondary}" CornerRadius="14" BorderBrush="{DynamicResource BorderDark}" BorderThickness="1" Padding="18" Margin="0,0,16,0">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="{Binding Loc[Dam_AddDamaged]}" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource PrimaryAccent}" Margin="0,0,0,14" />

                        <!-- Barcode Scan -->
                        <TextBlock Text="{Binding Loc[Gen_Barcode]}" FontSize="12" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,4" />
                        <Grid Margin="0,0,0,12">
                            <TextBox Style="{StaticResource ModernDarkTextBox}" Text="{Binding SearchBarcode, UpdateSourceTrigger=PropertyChanged}" Padding="8,6" FontSize="13">
                                <TextBox.InputBindings>
                                    <KeyBinding Key="Enter" Command="{Binding ScanOrSearchBarcodeCommand}" />
                                </TextBox.InputBindings>
                            </TextBox>
                            <Button Style="{StaticResource PrimaryAccentButton}" Command="{Binding ScanOrSearchBarcodeCommand}" HorizontalAlignment="Right" Padding="10,4" FontSize="11" Margin="3">
                                <TextBlock Text="{Binding Loc[Gen_Search]}" />
                            </Button>
                        </Grid>

                        <!-- Product Selector -->
                        <TextBlock Text="{Binding Loc[Pur_ProductName]}" FontSize="12" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,4" />
                        <ComboBox ItemsSource="{Binding ProductsList}" SelectedItem="{Binding SelectedProduct}" DisplayMemberPath="Name" 
                                  Height="38" Background="#1E293B" Foreground="#F8FAFC" FontSize="13" Margin="0,0,0,12" />

                        <!-- Quantity -->
                        <TextBlock Text="{Binding Loc[Gen_Quantity]}" FontSize="12" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,4" />
                        <TextBox Style="{StaticResource ModernDarkTextBox}" Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged, StringFormat='{}{0:0}'}" 
                                 TextAlignment="Center" FontSize="16" FontWeight="Bold" Height="38" Margin="0,0,0,12" />

                        <!-- Reason -->
                        <TextBlock Text="{Binding Loc[Dam_Reason]}" FontSize="12" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,4" />
                        <ComboBox SelectedValue="{Binding Reason, Mode=TwoWay}" SelectedValuePath="Content" Height="36" Background="#1E293B" Foreground="#F8FAFC" FontSize="13" Margin="0,0,0,12">
                            <ComboBoxItem Content="تالف / كسر" />
                            <ComboBoxItem Content="منتهي الصلاحية" />
                            <ComboBoxItem Content="سوء تخزين / رطوبة" />
                            <ComboBoxItem Content="فقدان / عجز" />
                            <ComboBoxItem Content="أخرى" />
                        </ComboBox>

                        <!-- Notes -->
                        <TextBlock Text="{Binding Loc[Gen_Notes]}" FontSize="12" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,4" />
                        <TextBox Style="{StaticResource ModernDarkTextBox}" Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}" Height="60" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,18" />

                        <!-- Save Damaged Button -->
                        <Button Style="{StaticResource DangerButton}" Command="{Binding SaveDamagedRecordCommand}" Height="44">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="⚠️ " FontSize="16" />
                                <TextBlock Text="{Binding Loc[Dam_AddDamaged]}" FontSize="13" FontWeight="Bold" />
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <!-- Damaged Items List Table -->
            <Border Grid.Column="1" Background="{DynamicResource BgDarkSecondary}" CornerRadius="14" BorderBrush="{DynamicResource BorderDark}" BorderThickness="1" Padding="14">
                <DataGrid ItemsSource="{Binding DamagedList}" AutoGenerateColumns="False" 
                          IsReadOnly="True" HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                          Background="Transparent" RowBackground="Transparent" BorderThickness="0"
                          CanUserResizeColumns="False" CanUserReorderColumns="False" CanUserResizeRows="False" CanUserSortColumns="False"
                          HorizontalGridLinesBrush="{DynamicResource BorderDark}"
                          RowHeight="44">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="تاريخ الإتلاف" Binding="{Binding CreatedAt, StringFormat='{}{0:yyyy/MM/dd - hh:mm tt}'}" Width="150" Foreground="{DynamicResource TextSecondary}" />
                        <DataGridTextColumn Header="الباركود" Binding="{Binding Barcode}" Width="120" Foreground="{DynamicResource PrimaryAccent}" />
                        <DataGridTextColumn Header="اسم المادة" Binding="{Binding ProductName}" Width="2*" Foreground="{DynamicResource TextPrimary}" />
                        <DataGridTextColumn Header="الكمية" Binding="{Binding Quantity, StringFormat='{}{0:N0}'}" Width="70" Foreground="{DynamicResource DangerBrush}" />
                        <DataGridTextColumn Header="التكلفة/قطعة" Binding="{Binding UnitCost, StringFormat='{}{0:N0} د.ع'}" Width="100" Foreground="{DynamicResource TextSecondary}" />
                        <DataGridTextColumn Header="قيمة الخسارة" Binding="{Binding TotalLossAmount, StringFormat='{}{0:N0} د.ع'}" Width="110" Foreground="{DynamicResource WarningBrush}" />
                        <DataGridTextColumn Header="السبب" Binding="{Binding Reason}" Width="120" Foreground="{DynamicResource PrimaryAccent}" />
                        
                        <DataGridTemplateColumn Header="إجراء" Width="80">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Style="{StaticResource DangerButton}" 
                                            Command="{Binding DataContext.DeleteDamagedRecordCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                            CommandParameter="{Binding}" Padding="8,4" FontSize="11">
                                        <TextBlock Text="🗑 حذف" />
                                    </Button>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
