<UserControl x:Class="HamoPos.Views.StockView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="750" d:DesignWidth="1100"
             FlowDirection="RightToLeft">

    <Grid Margin="20,16">
        <Grid.RowDefinitions>
            <!-- Header & Search -->
            <RowDefinition Height="Auto" />
            <!-- KPI Stats Bar -->
            <RowDefinition Height="Auto" />
            <!-- Stock Table -->
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="📊 " FontSize="22" />
                <TextBlock Text="{Binding Loc[Stock_Title]}" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center" />
            </StackPanel>

            <Grid HorizontalAlignment="Right" Width="360">
                <TextBox Style="{StaticResource ModernDarkTextBox}" Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" 
                         Padding="12,8" FontSize="14" />
                <TextBlock Text="{Binding Loc[Inv_SearchPlaceholder]}" Foreground="{DynamicResource TextSecondary}" FontSize="13" 
                           IsHitTestVisible="False" VerticalAlignment="Center" Margin="14,0,0,0" HorizontalAlignment="Left">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SearchQuery}" Value="">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
        </Grid>

        <!-- KPI Stats Bar -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{DynamicResource BgDarkSecondary}" CornerRadius="12" BorderBrush="{DynamicResource BorderDark}" BorderThickness="1" Padding="16" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock Text="{Binding Loc[Stock_TotalRegistered]}" FontSize="12" Foreground="{DynamicResource TextSecondary}" />
                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                        <TextBlock Text="{Binding TotalItemsCount, StringFormat='{}{0:N0}'}" FontSize="22" FontWeight="Black" Foreground="{DynamicResource PrimaryAccent}" />
                        <TextBlock Text="{Binding Loc[Stock_ItemUnit], StringFormat=' {0}'}" FontSize="13" Foreground="{DynamicResource TextSecondary}" VerticalAlignment="Bottom" Margin="4,0,0,3" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Background="{DynamicResource BgDarkSecondary}" CornerRadius="12" BorderBrush="{DynamicResource BorderDark}" BorderThickness="1" Padding="16" Margin="4,0">
                <StackPanel>
                    <TextBlock Text="{Binding Loc[Stock_TotalPieces]}" FontSize="12" Foreground="{DynamicResource TextSecondary}" />
                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                        <TextBlock Text="{Binding TotalPiecesStock, StringFormat='{}{0:N0}'}" FontSize="22" FontWeight="Black" Foreground="{DynamicResource SuccessBrush}" />
                        <TextBlock Text="{Binding Loc[Stock_PieceUnit], StringFormat=' {0}'}" FontSize="13" Foreground="{DynamicResource TextSecondary}" VerticalAlignment="Bottom" Margin="4,0,0,3" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="{DynamicResource BgDarkSecondary}" CornerRadius="12" BorderBrush="{DynamicResource BorderDark}" BorderThickness="1" Padding="16" Margin="8,0,0,0">
                <StackPanel>
                    <TextBlock Text="{Binding Loc[Stock_TotalCartons]}" FontSize="12" Foreground="{DynamicResource TextSecondary}" />
                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                        <TextBlock Text="{Binding TotalCartonsStock, StringFormat='{}{0:N0}'}" FontSize="22" FontWeight="Black" Foreground="{DynamicResource WarningBrush}" />
                        <TextBlock Text="{Binding Loc[Stock_CartonUnit], StringFormat=' {0}'}" FontSize="13" Foreground="{DynamicResource TextSecondary}" VerticalAlignment="Bottom" Margin="4,0,0,3" />
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Stock Table (Fixed Non-Resizable Columns) -->
        <Border Grid.Row="2" Background="{DynamicResource BgDarkSecondary}" CornerRadius="14" BorderBrush="{DynamicResource BorderDark}" BorderThickness="1" Padding="14">
            <DataGrid ItemsSource="{Binding StockList}" AutoGenerateColumns="False" 
                      IsReadOnly="True" HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                      Background="Transparent" RowBackground="Transparent" BorderThickness="0"
                      CanUserResizeColumns="False" CanUserReorderColumns="False" CanUserResizeRows="False" CanUserSortColumns="False"
                      HorizontalGridLinesBrush="{DynamicResource BorderDark}"
                      MouseDoubleClick="DataGrid_MouseDoubleClick"
                      RowHeight="48">
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Cursor" Value="Hand" />
                        <Setter Property="Background" Value="Transparent" />
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1E293B" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="الباركود" Binding="{Binding Barcode}" Width="140" Foreground="{DynamicResource PrimaryAccent}" FontWeight="Bold" FontSize="13" />
                    <DataGridTextColumn Header="اسم المادة" Binding="{Binding Name}" Width="2*" Foreground="{DynamicResource TextPrimary}" FontWeight="Bold" FontSize="14" />
                    <DataGridTextColumn Header="المندوب / المورد" Binding="{Binding SupplierName}" Width="140" Foreground="{DynamicResource TextSecondary}" FontSize="12" />
                    <DataGridTextColumn Header="سعر البيع المفرد" Binding="{Binding Price, StringFormat='{}{0:N0} د.ع'}" Width="130" Foreground="{DynamicResource SuccessBrush}" FontWeight="Black" FontSize="13" />
                    <DataGridTextColumn Header="الكمية المتبقية (قطع)" Binding="{Binding StockQuantity, StringFormat='{}{0:N0}'}" Width="140" Foreground="{DynamicResource WarningBrush}" FontWeight="Black" FontSize="14" />
                    <DataGridTextColumn Header="الكراتين المتبقية" Binding="{Binding CartonsCount, StringFormat='{}{0:N0}'}" Width="120" Foreground="{DynamicResource PrimaryAccent}" FontWeight="Bold" FontSize="13" />
                    
                    <DataGridTemplateColumn Header="حالة المخزون" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="6" Padding="8,3" HorizontalAlignment="Left">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="#064E3B" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding StockQuantity}" Value="0">
                                                    <Setter Property="Background" Value="#450A0A" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="متوفر بالمخزن" FontSize="11" FontWeight="Bold" Foreground="#34D399" HorizontalAlignment="Center">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding StockQuantity}" Value="0">
                                                        <Setter Property="Text" Value="نفد المخزون ⚠️" />
                                                        <Setter Property="Foreground" Value="#F87171" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Edit Product Action Column -->
                    <DataGridTemplateColumn Header="تعديل المادة" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Style="{StaticResource PrimaryAccentButton}" 
                                        Command="{Binding DataContext.EditProductCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        Padding="10,4" FontSize="11">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="✏️ " FontSize="11" />
                                        <TextBlock Text="تعديل" FontWeight="Bold" />
                                    </StackPanel>
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
